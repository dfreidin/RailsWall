<h1>Sign in</h1>
<% if flash[:error] %>
    <% flash[:error].each do |e| %>
        <p class="error"><%= e %></p>
    <% end %>
<% end %>
<%= form_for :user, url: {action: "create"} do |f| %>
    <%= f.text_field :username %>
    <%= submit_tag "Sign In" %>
<% end %>